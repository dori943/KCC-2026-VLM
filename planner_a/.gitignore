# Python
__pycache__/
*.py[cod]
.venv/
venv/

# VLM 원본 응답 로그 — 호출마다 갱신되고 용량이 커진다
vlm_logs/

# 로컬 환경
.env
.DS_Store

# 실험 결과·정리 대기 파일
_to_delete/

# outputs/ 와 outputs_vlm/ 은 커밋한다.
#   outputs/      규칙 판정. 결정론적이라 diff 가 회귀 검증이 된다
#   outputs_vlm/  VLM 판정. Planner B 로 넘기는 실제 전달물
# 둘 다 있어야 어블레이션 비교가 저장소 안에서 재현된다.
